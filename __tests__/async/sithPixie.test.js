// @flow

// import {startPixie} from 'redux-pixies'

// import sithPixie from '../../src/async/sithPixie'

describe('sith pixie', () => {

  describe('should request sith', () => {
    test('3616', () => {
      // const id = 3616
      // const dispatch = () => {}
      // const testPixie = startPixie(sithPixie)
      // testPixie.update({id, dispatch})
    })
  })

})
